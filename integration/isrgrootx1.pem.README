The isrgrootx1.pem file is one of Let's encrypt's root certificates, copied from
https://letsencrypt.org/certs/isrgrootx1.pem.  It is the root certificate used
for https://st.glasklar.is/, which hosts QA images.
